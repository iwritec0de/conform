# CLI integration tests use OAuth via `claude` CLI — no API key needed.
# These vars set defaults for run-integration.sh flags.

# Optional: Default model (haiku, sonnet, opus)
# TEST_MODEL=haiku

# Optional: Max turns per test (default: 3)
# TEST_MAX_TURNS=3

# Optional: Per-test timeout in seconds
# TEST_TIMEOUT=60
