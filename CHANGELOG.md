# Changelog

## 0.2.0

- Fix missing `ajv` and `ajv-formats` runtime dependencies in published package
- Add `"types": ["node"]` to tsconfig for TypeScript 6.x compatibility
- Remap `-v` flag to `--version` (`--verbose` is now long-form only)
- Add demo gif to README

## 0.1.0

Initial release.
